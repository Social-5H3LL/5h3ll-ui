---
templateEngineOverride: njk
layout: layouts/page.njk
title: llms.txt
description: Machine-readable documentation index for AI agents — preview the generated llms.txt and llms-full.txt exports.
icon: file-text
toc:
  - label: Overview
    id: overview
  - label: Index preview
    id: index-preview
  - label: Full export
    id: full-export
  - label: Usage
    id: usage
---

{% from "macros/code_block.njk" import code_block %}

<h2 id="overview"><a href="#overview">Overview</a></h2>

<div class="prose">
  <p>Each docs build generates plain-text exports for coding agents and IDE context tools. This page previews the content served at the site root.</p>
  <ul>
    <li><a href="/llms.txt" hx-boost="false"><code>/llms.txt</code></a> — compact index of all documentation pages</li>
    <li><a href="/llms-full.txt" hx-boost="false"><code>/llms-full.txt</code></a> — full stripped markdown for every page ({{ llmsContent.docCount }} docs, {{ llmsContent.fullLineCount }} lines)</li>
  </ul>
  <p>Generated by <code>src/eleventy/llm-content.js</code> from <code>docs.json</code> menu slugs and all <code>docs/src/**/*.md</code> sources.</p>
</div>

<h2 id="index-preview"><a href="#index-preview">Index preview</a></h2>

<div class="prose">
  <p>The index below matches <a href="/llms.txt" hx-boost="false"><code>/llms.txt</code></a> from the latest build. Copy it or point agents at the raw URL.</p>
</div>

<div class="llms-preview">
  {{ code_block(llmsContent.indexText, "text", "my-6") }}
</div>

<h2 id="full-export"><a href="#full-export">Full export</a></h2>

<div class="prose">
  <p>The full export concatenates every documentation page with titles, source URLs, and stripped markdown bodies. It is large — use the raw file for agents instead of copying from the browser.</p>
  <p><a href="/llms-full.txt" class="btn btn-sm" hx-boost="false">Open llms-full.txt</a></p>
</div>

<h2 id="usage"><a href="#usage">Usage</a></h2>

<div class="prose">
  <p>Typical agent setup:</p>
  <ol>
    <li>Load <code>/llms.txt</code> for navigation and page discovery.</li>
    <li>Load <code>/llms-full.txt</code> when the agent needs complete page bodies in one file.</li>
    <li>Load <a href="https://github.com/social-5h3ll/5h3ll-ui/blob/main/agents/SKILL.md"><code>agents/SKILL.md</code></a> for markup and install constraints.</li>
    <li>Open individual component pages for the task at hand.</li>
  </ol>
  <p>See also <a href="/integrations/">Integrations</a> for supported agent tools.</p>
</div>
